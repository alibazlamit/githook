{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "cd d:/githook && make vet 2>&1 || true",
            "timeout": 30,
            "statusMessage": "Running go vet..."
          }
        ]
      }
    ]
  }
}
